export default class Home {
    id: string;
    name: string;
    size: number;

    constructor(id: string, name: string, size: number) {
        this.id = id;
        this.name = name;
        this.size = size;
    }
}